<div id="comment-edit">
  <h4><%= t('.title') %></h4>

  <%= form_tag(:action => "update") do %>
    <%= hidden_field_tag "id", params[:id] %>
    <%= text_area("comment", "body", :rows => 10, :cols => 60) %><br>
    <%= submit_tag t('.save') %>
  <% end %>
</div>
