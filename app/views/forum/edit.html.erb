<div id="preview" style="display: none; margin: 1em 0; width: 60em;">
</div>

<%= form_tag(:action => "update") do %>
  <%= hidden_field_tag "id", params[:id] %>
  <table>
    <tr><td><label for="forum_post_title"><%=t :forum_title %></label></td><td><%= text_field "forum_post", "title", :size => 60 %></td></tr>
    <tr><td colspan="2"><%= text_area "forum_post", "body", :rows => 20, :cols => 80 %></td></tr>
    <tr><td colspan="2">
      <%= submit_tag t(:forum_post) %>
      <%= submit_to_remote "preview", t(:forum_preview), :url => {:action => "preview"}, :method => :get, :update => "preview", :success => "$('preview').show()" %>
    </td></tr>
  </table>
<% end %>
