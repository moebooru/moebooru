<%=
#if(@post.rating == "e" || @post.rating == "q" || @post.tags.include?("swimsuits") || @post.tags.include?("bra") || @post.tags.include?("pantsu") || @post.tags.include?("undressing"))
        print_advertisement("vertical")
#else
#end
%>
