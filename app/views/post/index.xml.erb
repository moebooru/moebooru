<?xml version="1.0" encoding="UTF-8"?>
<posts count="<%= @posts.total_entries %>" offset="<%= (@posts.current_page - 1) * @posts.per_page %>">
	<% @posts.each do |post| %>
		<%= post.to_xml(:skip_instruct => true) %>
	<% end %>
</posts>
